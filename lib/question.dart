class Question {
  String questionText;
  bool questionAnswer;

  Question({String q, bool a}) {
    questionText = q;
    questionAnswer = a;
  }
}

// create a object newQuestion

//Question newQuestion = Question('text', true);
